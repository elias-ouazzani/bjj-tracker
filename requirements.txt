nicegui>=2.24
firebase-admin>=6.5
itsdangerous>=2.2
pydantic>=2.10,<3
pydantic-ai
httpx>=0.27        # HTTP client for calling the Google Calendar REST API
tzdata             # IANA timezone DB for zoneinfo (Windows ships none; needed for ZoneInfo)
python-dotenv>=1.0
posthog>=3.5        # server-side product analytics (optional; off without POSTHOG_KEY)
pytest>=8.2
pytest-cov>=5.0
pytest-asyncio>=0.23
