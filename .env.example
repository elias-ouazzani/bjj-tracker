# Firebase / Firestore project ID (also the Firebase Auth project).
FIRESTORE_PROJECT_ID=your-firebase-project-id

# Anthropic API key for Pydantic AI training-tip generation.
ANTHROPIC_API_KEY=your-anthropic-api-key

# Path to the Firebase service-account JSON. LOCAL ONLY — on Cloud Run the
# attached service account is used and this must be left unset.
GOOGLE_APPLICATION_CREDENTIALS=/absolute/path/to/serviceAccount.json

# Signs the NiceGUI session cookie AND the strain_auth cookie. If unset it
# falls back to an insecure dev default, so set a long random value. Use the
# SAME value locally and in prod or existing login cookies stop verifying.
#   python3 -c "import secrets; print(secrets.token_urlsafe(48))"
STORAGE_SECRET=change-me-to-a-long-random-string

# Logging verbosity. DEBUG surfaces verify_id_token success + client auth
# breadcrumbs (auth.clientlog). Use DEBUG while debugging auth, INFO otherwise.
LOG_LEVEL=DEBUG

# --- Feature flags (PostHog) ---
# Without these, all flags default OFF (safe). See services/flags.py.
# Project API key (from PostHog → Settings → Project → API keys).
POSTHOG_API_KEY=phc_your_project_api_key
# Personal API key — enables fast local flag evaluation (recommended).
POSTHOG_PERSONAL_API_KEY=phx_your_personal_api_key
# Our PostHog project is in the US region. (EU cloud: https://eu.i.posthog.com)
POSTHOG_HOST=https://us.i.posthog.com

# Local dev override: force a single flag on without PostHog. Pattern:
# FLAG_<KEY>=1  (e.g. the training-streak feature below)
# FLAG_TRAINING_STREAK=1

# --- Admin access ---
# Comma-separated allow-list for the /admin page (case-insensitive). Each entry
# is a full email OR a whole domain with a leading @. Unset = nobody is admin.
#   "@atheal.com"            -> anyone @atheal.com
#   "@atheal.com,me@x.com"   -> the domain plus one extra person
ADMIN_EMAILS=@atheal.com
